spring:
    profiles:
        active: dev
    application:
        name: limits-service
    cloud:
        config:
            uri: http://localhost:8888
eureka:
    client:
        service-url:
            default-zone: localhost:8761/eureka

